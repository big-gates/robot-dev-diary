# colcon / ROS 2
build/
install/
log/

# CMake 빌드 폴더 — 이름 변형(bbuild, build-debug 등)도 전부 차단
*build*/
cmake-build-*/
_deps/
CMakeCache.txt
CMakeFiles/

# IDE
.idea/
*.iml
# .vscode는 공유 설정(tasks/launch)만 포함
.vscode/*
!.vscode/tasks.json
!.vscode/launch.json

# macOS
.DS_Store

# 컴파일 산출물
*.o
*.a
*.so
compile_commands.json

# 생성된 시각화 (필요하면 저널용으로 특정 파일만 git add -f)
*.svg
